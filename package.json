{
  "name": "pomodoro-agent",
  "version": "1.0.0",
  "description": "Pomodoro technique with AI agent for task management",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "biome lint ./src --apply",
    "format": "biome format ./src --write",
    "check": "biome check ./src --apply",
    "clean": "rimraf dist .wrangler node_modules"
  },
  "dependencies": {
    "hono": "^3.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@cloudflare/workers-types": "^4.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@8.0.0"
}
